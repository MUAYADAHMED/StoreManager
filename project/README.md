# 🍽️ StoreManager - دفتر حسابات البوفية

نظام إدارة حسابات متكامل وسهل الاستخدام للغاية لإدارة بوفيهات ومحلات البيع الصغيرة.

## 🌟 المزايا الرئيسية

✅ **سهولة الاستخدام**: واجهة بسيطة جداً وودية الاستخدام  
✅ **إدارة العملاء**: إضافة وتعديل وحذف العملاء بسهولة  
✅ **الفواتير والمدفوعات**: تسجيل فواتير ودفعات العملاء  
✅ **كشف الحساب**: عرض سجل كامل للعمليات  
✅ **إحصائيات**: ملخص شامل للمبيعات والديون  
✅ **مشاركة واتساب**: إرسال الحسابات مباشرة عبر الواتساب  
✅ **حفظ تلقائي**: البيانات تُحفظ تلقائياً في data/data.json  
✅ **بدون متطلبات**: لا Python، لا خوادم معقدة، فقط المتصفح!  
✅ **متوافق مع جميع الأجهزة**: موبايل - تابلت - ديسكتوب  

## 🚀 البدء السريع

### ⚡ الطريقة الأفضل: تشغيل الخادم

#### 1️⃣ تثبيت Node.js (مرة واحدة)
```
1. اذهب: https://nodejs.org/
2. حمّل "LTS"
3. ثبّت البرنامج
4. أعد تشغيل الحاسوب
```

#### 2️⃣ تشغيل الخادم
```
اضغط بزر الفأرة الأيمن على run.bat واختر "تشغيل"

أو اكتب في Command Prompt:
node server.js
```

#### 3️⃣ افتح المتصفح
```
اكتب: http://localhost:8000
```

**الآن:**
- ✅ البيانات تُحفظ في data/data.json مباشرة
- ✅ عند إضافة عميل → يُحفظ في الملف
- ✅ عند إغلاق وفتح الصفحة → البيانات موجودة!

### ➖ الطريقة البديلة: بدون خادم
```
1. اضغط بزر الفأرة الأيمن على customers.html
2. اختر "فتح مع" → اختر المتصفح
```

**ملاحظة:** هذه الطريقة:
- ✅ تعمل لكن بدون حفظ في JSON
- ✅ البيانات تُحفظ في localStorage فقط

### 2️⃣ إضافة عميل أول
1. اضغط الزر الأخضر الكبير في الأسفل
2. أدخل اسم العميل ورقم الهاتف
3. اضغط "✅ إضافة"

### 3️⃣ إضافة فاتورة
1. انقر على بطاقة العميل
2. اضغط الزر الأزرق الكبير
3. اختر "📄 فاتورة"
4. أدخل المبلغ
5. اضغط "💾 حفظ"

### 4️⃣ استقبال دفعة
1. انقر على بطاقة العميل
2. اضغط الزر الأزرق الكبير
3. اختر "💰 دفع"
4. أدخل المبلغ المستقبل
5. اضغط "💾 حفظ"

## 📁 هيكل المشروع

```
project/
├── customers.html          # الصفحة الرئيسية - إدارة العملاء
├── customer-details.html   # صفحة تفاصيل العميل
├── employees.html          # إدارة الموظفين (قيد التطوير)
├── suppliers.html          # إدارة الموردين (قيد التطوير)
├── settings.html           # الإعدادات (قيد التطوير)
├── components/
│   ├── header.html         # رأس الصفحة
│   ├── navbar.html         # شريط التنقل
│   ├── floating-button.html # الزر العائم
│   └── transaction-modal.html # نافذة العمليات
├── js/
│   └── common.js           # الدوال المشتركة والعامة
├── data/
│   └── data.json           # قاعدة البيانات
└── DOCUMENTATION.md        # دليل الاستخدام الشامل
```

## 🎨 واجهة المستخدم

### الزر العائم (Floating Action Button)
الزر الأزرق الكبير في الأسفل يحتوي على:

| اللون | الوظيفة | الاختصار |
|-------|---------|---------|
| 🟢 أخضر | إضافة عميل | customer-add |
| 🟠 برتقالي | إضافة صنف | item-add |
| 🟣 بنفسجي | سجل العمليات | transaction-history |
| 🔴 وردي | الإحصائيات | statistics |

### بطاقة العميل
تعرض:
- اسم العميل والرقم
- الرصيد الحالي (أخضر = له رصيد ، أحمر = عليه ديون)
- أزرار الإجراءات:
  - 📄 عملية - لإضافة فاتورة أو دفعة
  - 💬 مشاركة - لإرسال الحساب عبر واتساب
  - 📊 كشف - لعرض سجل العمليات
  - ✏️ تعديل - لتحديث البيانات
  - 🗑️ حذف - لحذف العميل

## 💾 إدارة البيانات

### التخزين المحلي (localStorage)
- البيانات تُخزّن تلقائياً في ذاكرة المتصفح
- لا تحتاج إلى خادم أو إنترنت
- البيانات تبقى حتى تمسح ذاكرة المتصفح

### النسخة الاحتياطية
```javascript
// تحميل نسخة احتياطية
downloadDataAsJSON()

// استيراد نسخة احتياطية
uploadDataFromJSON(file)
```

## 🔧 التطوير والتخصيص

### إضافة صنف جديد
عدّل `data/data.json`:
```json
{
  "items": [
    { "name": "سندوتش بيض", "price": 200 },
    { "name": "شاي", "price": 100 }
  ]
}
```

### تخصيص الألوان
عدّل في رأس الملفات:
```javascript
colors: {
  primary: "#3B82F6",      // الأزرق الرئيسي
  secondary: "#10B981"     // الأخضر الثانوي
}
```

### إضافة موظف جديد
```javascript
appData.employees.push({
  id: Date.now(),
  name: "علي الطباخ",
  role: "شيف",
  salary: 5000
});
saveDataToLocalStorage();
```

## 🌐 المتطلبات

- متصفح حديث (Chrome, Firefox, Edge, Safari)
- JavaScript مفعّل
- localStorage مفعّل
- بدون الحاجة إلى إنترنت (يعمل أوفلاين)

## 📊 الوظائف الرئيسية

### في common.js
```javascript
// حفظ البيانات
saveDataToLocalStorage()
loadDataFromLocalStorage()
downloadDataAsJSON()

// التنبيهات
showNotification(message, type)

// الزر العائم
toggleFabMenu()
closeFabMenu()
```

### في customers.html
```javascript
// إدارة العملاء
addNewCustomer(event)
editCustomer(customerId)
deleteCustomer(customerId)
renderCustomers()

// العمليات
openTransactionModal(targetId, targetType)
saveTransaction()
calculateCustomerBalance(customerId)

// الكشوفات
showStatement(customerId)
shareViaWhatsApp(customerId)
```

## 🎯 حالات الاستخدام

### 📱 بوفيه صغير
مثالي لإدارة حسابات البوفيهات والمقاهي الصغيرة

### 🏪 محل بقالة
تتبع ديون العملاء والمدفوعات

### 👨‍💼 سمسار
تسجيل الفواتير والمدفوعات للعملاء

### 💼 عامل حر
إدارة حسابات الزبائن والعمليات

## 🐛 معالجة الأخطاء

### المشكلة: البيانات لا تُحفظ
```javascript
// تحقق من localStorage
console.log(localStorage.getItem('storeManagerData'))

// امسح وأعد تحميل
localStorage.clear()
location.reload()
```

### المشكلة: الأزرار لا تستجيب
```javascript
// تأكد من تحميل common.js
console.log(typeof saveDataToLocalStorage)

// تحقق من بقية البيانات
console.log(appData)
```

## 📚 الدوال المتاحة

| الدالة | الوصف |
|--------|-------|
| `loadData()` | تحميل البيانات من JSON |
| `saveDataToLocalStorage()` | حفظ البيانات محلياً |
| `loadDataFromLocalStorage()` | تحميل من الذاكرة المحلية |
| `downloadDataAsJSON()` | تحميل نسخة احتياطية |
| `uploadDataFromJSON(file)` | استيراد البيانات |
| `showNotification(msg, type)` | عرض تنبيه |
| `updateDate()` | تحديث التاريخ |

## 🎓 أمثلة الاستخدام

### إضافة عميل برمجياً
```javascript
appData.customers.push({
  id: Date.now(),
  name: "محمد أحمد",
  phone: "0777123456",
  transactions: []
});
saveDataToLocalStorage();
renderCustomers();
```

### إضافة فاتورة برمجياً
```javascript
const customer = appData.customers[0];
customer.transactions.push({
  id: Date.now(),
  type: "invoice",
  amount: 500,
  date: new Date().toISOString(),
  description: "فاتورة جديدة",
  items: []
});
saveDataToLocalStorage();
```

## 🔐 الأمان

⚠️ **ملاحظات أمان**:
- البيانات تُخزّن محلياً فقط
- لا يتم إرسال البيانات إلى أي خادم
- لا يوجد تشفير (استخدم HTTPS لأمان أفضل)
- احرص على عمل نسخ احتياطية دورية

## 📞 الدعم والمساعدة

للمزيد من المعلومات، اطلع على `DOCUMENTATION.md`

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 👨‍💻 المطورون

تطويره بحب ❤️ لتسهيل إدارة أعمالك

---

**آخر تحديث**: 19 فبراير 2026  
**الإصدار**: 1.0.0
